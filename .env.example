# No quotes around values. docker --env-file / compose env_file keep quotes
# literally (unlike python-dotenv), which corrupts the API key.
PORT=8000
API_KEY=your_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash
LLM_TIMEOUT_S=8
LLM_TEMPERATURE=0
